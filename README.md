# Countdown App written in Electron

This app will overlay on all workspaces in Mac OSX because we wanted an application to run over a full screened Google Sheets.

How to use:


npm install

npm run electron
